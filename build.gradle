buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.3.0'
    }
}

ext {
    compileSdkVersion = 25
    buildToolsVersion = "25.0.0"
}

allprojects {
    repositories {
        mavenCentral()
    }
}
